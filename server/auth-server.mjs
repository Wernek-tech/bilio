import http from 'node:http';import crypto from 'node:crypto';import fs from 'node:fs';import path from 'node:path';import {createRoom,roomByCode,roomForUser,joinRoom,leaveRoom,toggleReady,publicRoom,matches,privateMatch,invites,sys,finishVote} from './vampire-store.mjs';
const PORT=Number(process.env.BILIO_API_PORT||8787),dbPath=path.resolve(process.env.BILIO_DB_PATH||'server/data.json');fs.mkdirSync(path.dirname(dbPath),{recursive:true});let db={users:[],sessions:{}};try{db=JSON.parse(fs.readFileSync(dbPath,'utf8'))}catch{};db.users ||= [];db.sessions ||= {};for(const u of db.users){u.gold ??= 0;u.diamonds ??= 0;u.messages ??= [];u.notifications ??= [];u.registrationRewardGranted ??= false;}const save=()=>{const tmp=dbPath+'.tmp';fs.writeFileSync(tmp,JSON.stringify(db,null,2));fs.renameSync(tmp,dbPath)};const reserved=new Set(['admin','administrator','system','biliobot']);const attempts=new Map(),chatRate=new Map();const norm=s=>String(s||'').trim().toLocaleLowerCase('tr-TR').replace(/ı/g,'i');const hash=p=>new Promise((ok,no)=>{const salt=crypto.randomBytes(16);crypto.scrypt(p,salt,64,(e,k)=>e?no(e):ok(`${salt.toString('hex')}:${k.toString('hex')}`))});const verify=(p,h)=>new Promise(ok=>{const [s,k]=h.split(':');crypto.scrypt(p,Buffer.from(s,'hex'),64,(e,d)=>ok(!e&&crypto.timingSafeEqual(d,Buffer.from(k,'hex'))))});const cookies=req=>Object.fromEntries((req.headers.cookie||'').split(';').filter(Boolean).map(x=>x.trim().split('=')));const publicUser=u=>({id:u.id,username:u.username,level:u.level,xp:u.xp,selectedTitleId:u.selectedTitleId,gold:u.gold||0,diamonds:u.diamonds||0,unreadMessages:(u.messages||[]).filter(x=>!x.readAt).length,unreadNotifications:(u.notifications||[]).filter(x=>!x.readAt).length});function send(res,status,obj,headers={}){res.writeHead(status,{'Content-Type':'application/json; charset=utf-8',...headers});res.end(JSON.stringify(obj))}async function body(req){let s='';for await(const c of req){s+=c;if(s.length>20000)throw Error('too large')}return JSON.parse(s||'{}')}const clean=s=>String(s||'').trim().slice(0,400).replace(/[<>]/g,'');const rate=(key,ms=700)=>{const n=Date.now(),last=chatRate.get(key)||0;if(n-last<ms)throw Error('Çok hızlı gönderiyorsun.');chatRate.set(key,n)};
const server=http.createServer(async(req,res)=>{try{const url=new URL(req.url,'http://x');if(!url.pathname.startsWith('/api/'))return send(res,404,{error:'Bulunamadı.'});const sid=cookies(req).bilio_session,uid=sid&&db.sessions[sid]?.userId,me=db.users.find(u=>u.id===uid);if(url.pathname==='/api/me'&&req.method==='GET')return me?send(res,200,{user:publicUser(me)}):send(res,401,{error:'Oturum yok.'});if(url.pathname==='/api/register'&&req.method==='POST'){const ip=req.socket.remoteAddress||'x',now=Date.now(),key='register:'+ip,a=(attempts.get(key)||[]).filter(t=>now-t<60000);if(a.length>=6)return send(res,429,{error:'Çok fazla kayıt denemesi. Biraz sonra tekrar deneyin.'});a.push(now);attempts.set(key,a);const b=await body(req),raw=String(b.username||'').trim(),n=norm(raw);if(!raw)return send(res,400,{error:'Kullanıcı adı zorunludur.'});if(n.length<3||n.length>20||!/^[-_\p{L}\p{N}]+$/u.test(raw)||reserved.has(n))return send(res,400,{error:'Kullanıcı adı 3–20 karakter olmalı ve harf, rakam, tire veya alt çizgi içermelidir.'});if(db.users.some(u=>u.usernameNormalized===n))return send(res,409,{error:'Bu kullanıcı adı zaten kullanılıyor.'});if(typeof b.password!=='string'||b.password.length<8||b.password.length>128)return send(res,400,{error:'Şifre en az 8 karakter olmalıdır.'});if(b.password!==b.passwordRepeat)return send(res,400,{error:'Şifreler eşleşmiyor.'});const createdAt=new Date().toISOString(),u={id:crypto.randomUUID(),username:raw,usernameNormalized:n,passwordHash:await hash(b.password),createdAt,lastLoginAt:createdAt,level:1,xp:0,selectedTitleId:'title-1',gold:5000,diamonds:1000,registrationRewardGranted:true,registrationRewardGrantedAt:createdAt,profile:{createdAt,statusText:'',avatarUrl:'',selectedFrameId:null,selectedBadgeIds:[]},messages:[{id:crypto.randomUUID(),sender:'Bilio',title:"Bilio'ya hoş geldin!",body:'Kaydolduğunuz için teşekkür ederiz. Bilio dünyasında keyifli oyunlar ve iyi eğlenceler dileriz!',details:'Kayıt ödülleri: 5.000 altın ve 1.000 elmas',createdAt,readAt:null}],notifications:[{id:crypto.randomUUID(),title:'Kayıt ödüllerin hazır!',body:'5.000 altın ve 1.000 elmas hesabına eklendi.',createdAt,readAt:null}]};const sidNew=crypto.randomBytes(32).toString('hex');db.users.push(u);db.sessions[sidNew]={userId:u.id,createdAt:now};try{save()}catch(e){db.users=db.users.filter(x=>x.id!==u.id);delete db.sessions[sidNew];throw e}return send(res,201,{user:publicUser(u)},{'Set-Cookie':`bilio_session=${sidNew}; HttpOnly; SameSite=Lax; Path=/; Max-Age=604800${process.env.NODE_ENV==='production'?'; Secure':''}`})}if(url.pathname==='/api/login'&&req.method==='POST'){const ip=req.socket.remoteAddress||'x',now=Date.now(),a=(attempts.get(ip)||[]).filter(t=>now-t<60000);if(a.length>=8)return send(res,429,{error:'Çok fazla giriş denemesi. Biraz sonra tekrar deneyin.'});const b=await body(req),u=db.users.find(x=>x.usernameNormalized===norm(b.username));if(!u||!await verify(String(b.password||''),u.passwordHash)){a.push(now);attempts.set(ip,a);return send(res,401,{error:'Kullanıcı adı veya şifre hatalı.'})}u.lastLoginAt=new Date().toISOString();const s=crypto.randomBytes(32).toString('hex');db.sessions[s]={userId:u.id,createdAt:now};save();return send(res,200,{user:publicUser(u)},{'Set-Cookie':`bilio_session=${s}; HttpOnly; SameSite=Lax; Path=/; Max-Age=604800${process.env.NODE_ENV==='production'?'; Secure':''}`})}if(url.pathname==='/api/logout'&&req.method==='POST'){if(sid)delete db.sessions[sid];save();return send(res,200,{ok:true},{'Set-Cookie':'bilio_session=; HttpOnly; SameSite=Lax; Path=/; Max-Age=0'})}if(url.pathname==='/api/messages'&&req.method==='GET'){if(!me)return send(res,401,{error:'Giriş gerekli.'});return send(res,200,{items:[...(me.messages||[])].sort((a,b)=>b.createdAt.localeCompare(a.createdAt))})}if(url.pathname==='/api/notifications'&&req.method==='GET'){if(!me)return send(res,401,{error:'Giriş gerekli.'});return send(res,200,{items:[...(me.notifications||[])].sort((a,b)=>b.createdAt.localeCompare(a.createdAt))})}const readMatch=url.pathname.match(/^\/api\/(messages|notifications)\/([^/]+)\/read$/);if(readMatch&&req.method==='POST'){if(!me)return send(res,401,{error:'Giriş gerekli.'});const list=readMatch[1]==='messages'?(me.messages||[]):(me.notifications||[]),item=list.find(x=>x.id===readMatch[2]);if(!item)return send(res,404,{error:'Kayıt bulunamadı.'});item.readAt ||= new Date().toISOString();save();return send(res,200,{ok:true,user:publicUser(me)})}if(!me&&url.pathname.startsWith('/api/game/'))return send(res,401,{error:'Giriş gerekli.'});if(url.pathname==='/api/game/vampire/active'&&req.method==='GET'){const r=roomForUser(me.id);return send(res,200,{room:r?publicRoom(r):null,match:r?.matchId?privateMatch(matches.get(r.matchId),me.id):null})}if(url.pathname==='/api/game/vampire/create'&&req.method==='POST'){const old=roomForUser(me.id);return send(res,200,{room:publicRoom(old||createRoom(me))})}if(url.pathname==='/api/game/vampire/join'&&req.method==='POST'){const b=await body(req),r=roomByCode(b.code);if(!r)return send(res,404,{error:'Oda artık mevcut değil.'});return send(res,200,{room:publicRoom(joinRoom(r,me))})}const r=roomForUser(me.id);if(url.pathname==='/api/game/vampire/leave'&&req.method==='POST'){if(r)leaveRoom(r,me.id);return send(res,200,{ok:true})}if(!r)return send(res,404,{error:'Aktif oda yok.'});if(url.pathname==='/api/game/vampire/ready'&&req.method==='POST')return send(res,200,{ready:toggleReady(r,me.id),room:publicRoom(r)});if(url.pathname==='/api/game/vampire/invite'&&req.method==='POST'){if(r.hostUserId!==me.id)return send(res,403,{error:'Yalnızca kurucu davet gönderebilir.'});if(Date.now()-(r.lastInviteAt||0)<15000)return send(res,429,{error:'Davet için biraz bekle.'});r.lastInviteAt=Date.now();const inv={id:crypto.randomUUID(),game:'VAMPİR KÖYLÜ',roomCode:r.code,inviter:me.username,players:r.players.length,max:12,createdAt:Date.now(),expiresAt:Date.now()+300000};invites.push(inv);return send(res,200,{invite:inv})}if(url.pathname==='/api/game/vampire/chat'&&req.method==='POST'){rate(`room:${me.id}`);const b=await body(req),c=clean(b.content);if(!c)return send(res,400,{error:'Boş mesaj gönderilemez.'});r.messages.push({id:crypto.randomUUID(),senderUserId:me.id,username:me.username,content:c,createdAt:Date.now()});return send(res,200,{ok:true})}if(!r.matchId)return send(res,409,{error:'Maç başlamadı.'});const m=matches.get(r.matchId),p=m.players[me.id];if(url.pathname==='/api/game/vampire/action'&&req.method==='POST'){const b=await body(req);if(m.phase!=='NIGHT'||!p.alive)return send(res,409,{error:'Bu işlem şu anda kullanılamaz.'});const t=m.players[b.targetId];if(!t?.alive)return send(res,400,{error:'Geçersiz hedef.'});if(p.role==='VILLAGER')return send(res,403,{error:'Gece yeteneğin yok.'});if(p.role==='VAMPIRE'&&t.role==='VAMPIRE')return send(res,400,{error:'Vampir takım arkadaşını seçemezsin.'});if(p.role==='SEER'&&b.targetId===me.id)return send(res,400,{error:'Kendini araştıramazsın.'});p.nightTarget=b.targetId;if(p.role==='SEER')return send(res,200,{ok:true,result:t.role==='VAMPIRE'?'VAMPİR':'VAMPİR DEĞİL'});return send(res,200,{ok:true})}if(url.pathname==='/api/game/vampire/vampire-chat'&&req.method==='POST'){rate(`vamp:${me.id}`);if(m.phase!=='NIGHT'||!p.alive||p.role!=='VAMPIRE')return send(res,403,{error:'Vampir sohbetine erişemezsin.'});const c=clean((await body(req)).content);if(!c)return send(res,400,{error:'Boş mesaj.'});m.vampireMessages.push({id:crypto.randomUUID(),username:me.username,content:c,createdAt:Date.now()});return send(res,200,{ok:true})}if(url.pathname==='/api/game/vampire/village-chat'&&req.method==='POST'){rate(`village:${me.id}`);if(m.phase!=='DAY_DISCUSSION'||!p.alive)return send(res,403,{error:'Şu anda köy sohbetine yazamazsın.'});const c=clean((await body(req)).content);if(!c)return send(res,400,{error:'Boş mesaj.'});m.villageMessages.push({id:crypto.randomUUID(),username:me.username,content:c,createdAt:Date.now()});return send(res,200,{ok:true})}if(url.pathname==='/api/game/vampire/vote'&&req.method==='POST'){if(m.phase!=='DAY_VOTING'||!p.alive)return send(res,403,{error:'Oy kullanamazsın.'});const b=await body(req);if(b.abstain)m.votes[me.id]={abstain:true};else{if(b.targetId===me.id||!m.players[b.targetId]?.alive)return send(res,400,{error:'Geçersiz oy hedefi.'});m.votes[me.id]={target:b.targetId,abstain:false}}const living=Object.values(m.players).filter(x=>x.alive).length;if(Object.keys(m.votes).length>=living)finishVote(m);return send(res,200,{ok:true})}return send(res,404,{error:'Bulunamadı.'})}catch(e){send(res,500,{error:e.message||'Sunucu hatası.'})}});server.listen(PORT,()=>console.log(`Bilio API http://localhost:${PORT}`));
