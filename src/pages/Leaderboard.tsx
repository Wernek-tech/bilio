import {useEffect,useState} from 'react';import SiteShell from '../components/SiteShell';import {api,useAuth} from '../auth/AuthContext';import {titles} from '../data/titles';
type Row={rank:number;userId:string;username:string;score:number;victories:number;titleId?:string;avatarUrl?:string};
export default function Leaderboard(){const auth=useAuth(),[rows,setRows]=useState<Row[]>([]),[mine,setMine]=useState<Row|null>(null),[loading,setLoading]=useState(true);useEffect(()=>{api<{rows:Row[];current:Row|null}>('/leaderboard?limit=100').then(x=>{setRows(x.rows);setMine(x.current)}).finally(()=>setLoading(false))},[auth.user?.id]);const top=rows.slice(0,3),rest=rows.slice(3);const podium=[top[1],top[0],top[2]];return <SiteShell><div className="page-body leaderboard-page">{loading?<div className="page-loading">Sıralama yükleniyor...</div>:rows.length===0?<div className="empty-state page-empty"><b>Bu hafta henüz sıralama oluşmadı.</b></div>:<><div className="podium">{podium.map((r,i)=>r&&<article key={r.userId} className={`podium-card p${r.rank}`}><span className="rank-no">{r.rank}</span><div className="pod-avatar">{r.avatarUrl?<img src={r.avatarUrl} alt=""/>:r.username[0]}</div><b>{r.username}</b>{r.titleId&&<img className="title-art" src={(titles.find(t=>t.id===r.titleId)||titles[0]).assetPath} alt="Kuşanılmış unvan"/>}<strong>{new Intl.NumberFormat('tr-TR').format(r.score)} PUAN</strong></article>)}</div><section className="rank-table"><header><span>#</span><span>OYUNCU</span><span>PUAN</span></header>{rest.map(r=><div className="rank-row" key={r.userId}><span>{r.rank}</span><div className="rank-player"><div className="mini-avatar">{r.avatarUrl?<img src={r.avatarUrl} alt=""/>:r.username[0]}</div><b>{r.username}</b>{r.titleId&&<img src={(titles.find(t=>t.id===r.titleId)||titles[0]).assetPath} alt="Kuşanılmış unvan"/>}</div><strong>{new Intl.NumberFormat('tr-TR').format(r.score)}</strong></div>)}{auth.user&&mine&&!rows.some(r=>r.userId===mine.userId)&&<div className="rank-row pinned"><span>{mine.rank}</span><div className="rank-player"><b>{mine.username}</b></div><strong>{new Intl.NumberFormat('tr-TR').format(mine.score)}</strong></div>}{auth.user&&!mine&&<div className="no-rank">Bu hafta henüz sıralama puanın bulunmuyor.</div>}</section></>}</div></SiteShell>}
